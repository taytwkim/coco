Here we show sample input/outputs for two different Scish -> Cish compilers.
Both compilers follow a similar strategy, but produce slightly different
outputs. This should give you a sense for some different possibilities for
the code that your compiler can produce.

`sample1_input.scish` -> `sample1_output.cish`             (Compiler A)

`sample1_input.scish` -> `sample1_output_alternate.cish`   (Compiler B)

`sample2_input.scish` -> `sample2_output.cish`             (Compiler A)

`sample2_input.scish` -> `sample2_output_alternate.cish`   (Compiler B)
